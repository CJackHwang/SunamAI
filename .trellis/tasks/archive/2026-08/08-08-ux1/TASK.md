# TASK-UX1 — 侧边栏默认展开 + Sunam的电脑默认终端 tab

## 背景（用户明确要求）

1. **右侧侧边栏默认展开半屏模式**（现在是 collapsed 折叠）
2. **"Sunam的电脑"默认打开"终端"tab**（现在是"电脑"tab），方便用户直接看终端容器环境 + 输入命令
3. 相关选择器（tab 绑定、布局状态）注意同步

## 物理边界

- contracts 一字不改
- 样式可微调（半屏宽度），但保持现有设计规范（暗色主题/专业克制）
- 零新增依赖

## 需求

### R1. 侧边栏默认半屏展开

- `Workspace.tsx:60` `useState<TerminalLayout>('collapsed')` → 默认 `'half'`（或对应"半屏"的枚举值——**检查 TerminalLayout 的枚举值**：collapsed/half/full 之类）
- 用户手动折叠后保持（localStorage 持久化当前选择——**检查现有布局持久化机制**，若有则复用；无则加）

### R2. "Sunam的电脑"默认"终端"tab

- 容器打开时默认激活"终端"tab（现在是"电脑"）
- 相关选择器同步：tab 状态、containerSegment 初始值、可能影响的其他状态
- 用户手动切换后保持（会话内）

### R3. 联动检查

- 侧边栏展开 + 终端 tab 的组合：进入容器视图直接看到终端（可输入命令），不用再点
- 若"电脑"tab 有特殊用途（Agent 操作展示），保持可切换（默认终端但可点回电脑）

## 质量门禁（节选）

1. `npx tsc -b` 0 错
2. `npx vitest run tests/unit/` 全绿（布局状态相关测试若有）
3. 浏览器实测：打开 → 侧边栏半屏展开 + 容器视图默认终端 tab（直接可输入命令）
4. `git diff --check` 干净

## 提交

`feat: 侧边栏默认半屏 + Sunam的电脑默认终端 tab`
